# This is an ugly policy file

bundle agent report_time(
                          hours # too few hours in a day
                          ,minutes, 
                          seconds
 )
# Bundle to report time
{
  reports: # report stuff
    any:: # do this no matter what
      "Time is $(hours):$(minutes):$(seconds)";
}
